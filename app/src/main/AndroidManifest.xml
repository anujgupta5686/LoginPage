<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginpage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginPage">
        <activity android:name=".recycleview"></activity>
        <activity android:name=".contactlist">

        </activity>
        <activity android:name=".listview"></activity>
        <activity android:name=".page2" />
        <activity android:name=".page1" />
        <activity android:name=".Login_Form2" />
        <activity android:name=".Signup_Form2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sigup" />
        <activity android:name=".login"></activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>